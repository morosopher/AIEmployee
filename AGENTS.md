# AI Employee 仓库协作指南

## 适用范围与规则优先级

本文件适用于整个仓库。进入 `backend/` 或 `frontend/` 工作时，还必须读取并遵守对应目录下的 `AGENTS.md`。

子目录规则用于细化本文件；若规则冲突，离目标文件最近的 `AGENTS.md` 优先，但子目录规则不得放宽本文件定义的 M2 范围、继承自 M1 的可信执行不变量、安全边界、数据真实性和人工审批要求。用户的明确指令始终高于仓库规则。

## 项目定位与当前范围

AI Employee 是面向个人长期使用的智能办公助手。M1「可信任务中心 + 每日办公简报」已经完成，当前实施目标是已批准的 M2：**可执行邮件与日历助手**。M2 在保留 M1 可恢复、可审计执行底座的前提下，增加 Google 与 Microsoft 邮件/日历的受控真实写入；它仍是助手工作台，不是完整邮箱或日历客户端。

M2 必须包含：

- 保留单管理员登录、安全 Cookie 会话、每日简报、对话、任务历史、执行时间线、Taskiq、LangGraph Checkpoint、Outbox、SSE、审计和崩溃恢复等 M1 能力。
- Google 与 Microsoft OAuth 连接管理，以及邮件、日历目录和各作用域的只读增量同步。
- 按连接区分 `mail.read`、`mail.send`、`calendar.read`、`calendar.write` 的渐进授权、能力状态、默认发送账户和默认日历。
- 本地加密邮件草稿、不可变版本、新邮件、回复和全部回复；未批准草稿不得写入供应商草稿箱。
- 非重复日程的创建、修改和恢复提案，包含确定性冲突建议、工作时间、会议缓冲、ETag 和修改前快照。
- 仅允许 `mail.send`、`calendar.create`、`calendar.update`、`calendar.restore` 四种类型化真实写操作；每次只批准一个冻结且哈希匹配的精确命令。
- 幂等 ToolExecution 认领、安全重试、只读结果核对、`reconciling`、`needs_attention`、人工结果确认和完整审计时间线。
- 统一操作中心、邮件与日程编辑器、结构化审批预览、连接能力和工作设置界面。

M2 明确不包含供应商草稿箱同步、邮件转发、附件、HTML/富文本邮件、通讯录、批量操作、日程删除或取消、重复日程写入、会议链接自动创建、参会人 Free/Busy、完整收件箱/日历客户端、本地 Exchange、共享邮箱或代理发送、文件上传、Qdrant、RAG、长期记忆、通用 Planner、通用工具或工具市场、产品多 Agent、多用户或公开注册、Kubernetes。除非用户先批准新的里程碑设计，不得突破这些边界。

项目事实来源：

- 当前产品与架构规格：`docs/superpowers/specs/2026-08-06-executable-mail-calendar-assistant-m2-design.md`
- 当前任务级实施计划：`docs/superpowers/plans/2026-08-06-executable-mail-calendar-assistant-m2.md`
- M1 规格与计划继续作为已交付基线和回归事实来源；只有 M2 规格明确变更的部分才被取代。

规格定义“为什么和必须满足什么”，实施计划定义“按什么顺序落地”。两者不一致时应停止并向用户说明，而不是自行选择。

## 目标目录与职责

```text
AIEmployee/
├── backend/                 # FastAPI、领域逻辑、任务编排、集成与 Worker
│   ├── src/ai_employee/
│   ├── migrations/
│   └── tests/
├── frontend/                # Vue SPA、Pinia 状态、REST/SSE 客户端与 E2E
│   ├── src/
│   └── e2e/
├── justfiles/               # 开发、测试、数据库、Docker 和运维 recipe
├── scripts/                 # 可审计的测试与运维脚本
├── docs/                    # 规格、计划、运行手册与决策记录
├── ops/                     # 可选可观测性配置
├── compose.yaml
├── compose.dev.yaml
├── Caddyfile
└── justfile                 # 唯一统一命令入口
```

仓库已具备 M1 实现。M2 缺少的目标文件、迁移或 recipe 应按当前实施计划补齐，不得为了临时可运行而建立第二套目录、命令或架构。

## 开发环境

标准环境如下：

- Python 3.12，由 `uv` 管理依赖和锁文件。
- Node.js 24 LTS，由 `pnpm` 管理依赖和锁文件。
- PostgreSQL、Redis、Docker Compose、Caddy。
- 根目录使用 `just` 统一管理安装、开发、测试、迁移和运维。

从 `.env.example` 创建本地配置；不得提交 `.env`、本地证书、数据库转储或任何真实凭据。不要混用 `pip`/Poetry 与 `uv`，也不要混用 npm/yarn 与 `pnpm`。

## 构建、测试与验证命令

以下是脚手架完成后的标准入口；若当前 recipe 尚未落地，应先完成实施计划中的工具链任务，不能假装命令已通过。

| 目的 | 命令 |
|---|---|
| 环境诊断 / 安装依赖 | `just doctor` / `just bootstrap` |
| 完整开发环境 | `just dev` |
| 仅基础设施 | `just infra-up` / `just infra-down` |
| 单独启动进程 | `just web` / `just api` / `just worker` / `just scheduler` |
| 快速单测 | `just test`、`just test-backend`、`just test-frontend` |
| 集成 / E2E | `just test-integration` / `just test-e2e` |
| 静态质量 | `just lint` / `just typecheck` / `just format` |
| 本地提交前检查 | `just check` |
| 完整发布门禁 | `just ci` |
| 数据库 | `just db-upgrade` / `just db-revision name` / `just db-reset` |
| 运行诊断 | `just logs service` / `just ps` / `just health` |
| 备份恢复 | `just backup` / `just restore file` |

先运行与修改最相关的聚焦测试，再运行更大范围验证。文档或纯配置修改至少执行 `git diff --check` 和相关静态检查；任何“已完成”“测试通过”或“可发布”的结论都必须有本轮新鲜命令输出支持。

## 全局编码规范

- 保持模块小而职责单一，优先清晰接口，不创建承担多个领域职责的“大文件”。
- 公共边界使用明确类型和稳定 Schema；不得用无约束字典绕过类型系统。
- 标识符、API 字段和稳定错误码使用英文；面向用户及项目协作文档默认可使用中文。
- 编写或修改代码时必须附带详细、格式统一的中文注释。模块、类、公共函数、复杂流程和关键配置使用语言或框架原生的文档注释格式，说明业务目的、输入输出、边界条件、状态变化、安全/幂等约束及不直观决策；行内注释解释“为什么”，不得只逐行翻译代码。代码行为变化时必须同步更新注释，注释中同样禁止出现凭据和真实个人数据。
- 代码、配置和文档统一 UTF-8，禁止无意义格式化、无关重命名和顺手重构。
- 新依赖必须有当前里程碑内的明确用途，检查许可证、维护状态和安全影响，并更新锁文件。
- 时间在服务端统一存储为 UTC，展示时按用户 IANA 时区转换；不得依赖宿主机本地时区推断业务日期。
- 供应商返回值先规范化为内部模型；不得让 Gmail、Google Calendar、Microsoft Graph、模型或队列 SDK 类型渗透到领域层和前端。

## 架构约束

系统采用模块化单体，但 API、Worker、Scheduler 是独立进程。依赖方向固定为：

```text
api / agents / integrations / workers
                  ↓
             application
                  ↓
               domain
```

必须遵守以下不变量：

- PostgreSQL 是身份、业务数据、任务、审计、Outbox 和 Checkpoint 的唯一真实来源。
- Redis 只承载队列、实时通知、短期缓存和协调；Redis 丢失后必须能从 PostgreSQL 恢复。
- FastAPI 请求进程不执行长任务；长任务创建返回 HTTP 202 和 `task_id`。
- LangGraph 只负责编排、分支、中断和恢复，领域规则必须位于可独立测试的领域或应用层。
- Taskiq 和外部调用按至少一次语义设计；任务、节点和工具调用必须幂等。
- 持久事件先写 PostgreSQL，再通过 Outbox 投递；Pub/Sub 消息不是业务事实。
- 所有真实外部写操作仅限四种已批准命令，并必须依次经过类型校验、规范序列化、AEAD 加密、哈希绑定、人工审批、幂等 ToolExecution 认领和结果核对。真实写入默认关闭；全局开关、供应商开关和连接能力缺一不可。
- 写请求只有在能够证明先前操作未应用时才允许重试；请求结果未知时只能进入只读核对或人工确认，禁止盲目重放。
- 云端模型通过供应商无关的 `ModelGateway` 使用；不得静默切换模型供应商或数据流向。

## 修改策略

1. 修改前读取目标路径上所有适用的 `AGENTS.md`、相关规格和实施计划，并检查 `git status`。
2. 行为变更先写失败测试并观察预期失败，再添加最小实现；文档修订先明确受影响的事实来源。
3. 一次修改只解决一个清晰问题，保留用户现有改动，不覆盖或清理无关文件。
4. 修改 API、数据库或事件协议时，同步更新调用方、类型、迁移、契约测试和文档。
5. 优先扩展现有端口和模式；引入新抽象前必须证明现有边界无法清晰表达需求。
6. 不为后续里程碑预建空壳、通用框架或未使用配置。

## 测试要求

- 所有功能和缺陷修复执行红—绿—重构流程；测试必须先证明旧实现会失败。
- 单元测试覆盖纯领域规则和状态转换；集成测试覆盖 PostgreSQL、Redis、Taskiq、LangGraph、OAuth、同步和 SSE 边界。
- 前后端契约必须使用脱敏或合成 fixture，CI 不访问真实 Google、Microsoft、模型或个人账号。
- 时间、ID、模型、外部工具和网络响应应可替换为 Fake，禁止依赖真实时间和不稳定网络。
- 必测高风险路径包括用户隔离、幂等、重复投递、安全重试、请求后崩溃恢复、审批篡改、能力撤销、ETag 冲突、未知写结果、人工确认竞争、SSE 重放、部分失败和敏感日志脱敏。
- 提交前运行 `just check`；完成一个可发布里程碑前运行完整 `just ci`、故障演练及与风险匹配的安全检查。

## Git 规范

- 一个提交只包含一个逻辑完整的改动，提交信息使用 Conventional Commits，例如 `feat: ...`、`fix: ...`、`test: ...`、`docs: ...`、`chore: ...`。
- 执行已批准实施计划时，按计划的任务边界和指定提交信息提交；其他情况下未经用户要求不要自动提交。
- 提交前检查 `git diff`、`git diff --check` 和相关测试结果，不提交生成物、报告、缓存或本地配置。
- 禁止强推、重写共享历史、破坏性 reset 或删除用户未提交改动。遇到重叠修改先停下并说明。
- 不把不同里程碑、无关重构或大规模格式化混入同一提交。

## 安全与隐私规则

- 严禁提交或输出 Token、Cookie、OAuth 凭据、模型密钥、主加密密钥、私钥、验证码、完整邮件正文或真实个人数据。
- Google 与 Microsoft OAuth 按连接能力渐进申请最小委托 scope；不得申请 Contacts、Gmail Draft、Microsoft `Mail.ReadWrite`、应用权限或目录管理权限来绕过 M2 边界。
- OAuth Token 使用带版本的 AEAD 应用层加密；模型密钥和应用主密钥通过 Secret 文件注入，不写数据库。
- 日志、异常、Trace 和 fixture 必须过滤 Authorization、Cookie、Token、密钥、完整正文及敏感 Prompt。
- 会话使用 HttpOnly、Secure、SameSite Cookie；修改类请求必须有 CSRF 防护；公网流量必须通过 TLS。
- 模型输入遵循最小披露原则，先在本地清洗签名、引用历史、跟踪内容和明显敏感模式。
- 审批只能授权被冻结且哈希匹配的精确载荷；载荷变化、过期、重复或版本冲突都必须拒绝。
- 测试、演示和开发默认使用 Fake 工具和合成数据；只有发布门禁中另行授权的专用测试环境，才可在三层写入开关和精确账户允许列表保护下连接专用 Google/Microsoft 测试账户。

## 数据库规则

- PostgreSQL Schema 通过 SQLAlchemy 2 与 Alembic 管理，禁止手工修改共享数据库来替代迁移。
- 所有用户拥有的数据必须直接包含非空 `user_id`，或通过受强约束的父实体归属用户；Repository 查询必须显式注入用户条件。
- 供应商对象至少使用 `connection_id + provider_object_id` 唯一约束；任务和工具执行使用稳定幂等键。
- 业务写入、任务创建、审计事件和 Outbox 在需要原子性时必须位于同一事务。
- `audit_events` 按追加写设计；普通应用角色不得更新或删除。保留清理由独立权限和明确任务执行。
- 迁移采用向前兼容策略，先扩展、迁移数据、切换读取，再删除旧结构；不得依赖生产环境破坏性回滚。
- `db-reset`、恢复和批量删除必须验证非生产环境并要求确认；执行前解析并复核精确目标。
- 原始邮件 MIME、附件和完整供应商响应不持久化；命令、正文、日程敏感内容和补偿快照遵守 M2 规格中的加密与保留周期。

## 文档规则

- 架构、范围或行为契约变化时，先更新对应规格或新增 ADR，再更新实施计划和代码。
- README、运行手册、`.env.example`、API/SSE 契约和 `just` 命令必须与实现同步。
- 命令示例必须可复制执行，注明工作目录、前置条件和破坏性影响；不得记录真实凭据或个人数据。
- Mermaid 图只用于表达确有帮助的架构、状态机和数据流，不用图替代关键文字约束。
- 文档中不保留未决事项、无负责人占位符或与当前里程碑矛盾的未来承诺。

## Agent 行为约束

- 默认用中文与用户协作，先报告结果和关键风险，再补充必要技术细节。
- 作出假设前先从仓库发现事实；无法安全推断且会实质改变结果时，停止并向用户确认。
- 不擅自扩大已批准的 M2、启用产品多 Agent 能力、增加其他供应商或动作类型、连接真实账号，或绕过写入开关、适用环境要求的账户允许列表和人工审批执行外部写操作。
- 除非用户或适用流程明确要求，不启动子 Agent 或并行代理工作。
- 不执行破坏性命令，不清理用户改动，不泄露敏感信息；需要额外权限或外部协调时明确请求授权。
- 遇到缺陷或测试失败先定位根因，不凭猜测连续打补丁；遇到审查意见先验证技术事实。
- 完成前必须运行新鲜验证并阅读完整输出；不能用“应该通过”代替证据。
- 最终交付说明修改了什么、如何验证、仍有哪些限制；不要把未执行的检查描述为已通过。
